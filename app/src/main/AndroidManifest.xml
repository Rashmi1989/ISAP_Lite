<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ibm.cio.gss.isap_lite"
    android:versionCode="20"
    android:versionName="1.1.10">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.SelectclientActivity"
            android:theme="@style/Select_clientTheme" />
        <activity
            android:name=".activity.LoginActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.ClientsActivity" />
        <activity
            android:name=".activity.IsapMenuActivity"
            android:label="@string/title_activity_isap_menu"
            android:theme="@style/AppTheme" />
        <activity android:name=".activity.RequestAccessActivity" />
        <activity android:name=".activity.NewGoalActivity" />
        <activity android:name=".activity.NewInitiativeActivity" />
        <activity android:name=".activity.NewRelationshipActivity" />
    </application>

</manifest>